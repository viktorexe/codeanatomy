'use client'

export function VisualizationCanvas() {
  // This component is handled by the main interface HTML template
  // All functionality is managed through the MainInterface component
  return null
}